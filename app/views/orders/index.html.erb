<div class="flat_area grid_16">
	<h2><%= @title %></h2>
	<% if current_user.is_admin? or current_user.is_carrier? or current_user.is_onsite? or current_user.is_cpld? %>
		<%= render "search_options" %>
	<% end %>
	<div class="box grid_16 single_datatable">
		<% if current_user.is_doctor? %>
			<div id="dtdoctor" class="no_margin">
			<%= render "table_doctor" %>
		<% elsif current_user.is_onsite? %>
			<div id="dtonsite" class="no_margin">
			<%= render "table_onsite" %>
		<% elsif current_user.is_carrier? %>
			<div id="dtcarrier" class="no_margin">
			<%= render "table_carrier" %>
		<% elsif current_user.is_cpld? %>
			<div id="dtcarrier" class="no_margin">
			<%= render "table_cpld" %>
		<% else %>
			<div id="dtadmin" class="no_margin">
			<%= render "table_admin" %>
		<% end %>
		</div>
	</div>
</div>

<% if current_user.role != 5 %>
<div class="flat_area grid_16">
	<div class="align_right"><p><h2><%= link_to 'Add Order', new_order_path %></h2></p></div>
	<div><p><h2><%= link_to "Printable List (PDF)", orders_path(format: "pdf", datefrom: @datefrom, dateto: @dateto, origin: @origin, delivery_site: @delivery_site, ccc: @ccc, created_by: @created_by, sstatus: @sstatus) %></h2></p></div>
</div>
<% end %>